<h1>Todos#index</h1>

<table>
  <thead>
    <tr>
      <th>Todo</th>
      <th>Status</th>
    </tr>
  </thead>
  <tbody>
    <% @todos.each do |todo| %>
      <tr>
        <td><%= todo.title %></td>
        <td><%= todo.status %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<p><%= link_to "Add Todo", new_todo_path %></p>
